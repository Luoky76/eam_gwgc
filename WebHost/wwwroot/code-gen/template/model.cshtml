@using System;
@using System.Text;
@using RazorEngineCore
@using Gksyb.Common;
@using Gksyb.Common.Data
@inherits RazorEngineTemplateBase<DbTableInfo>
//------------------------------------------------------------------------------
// <auto-generated>
//     此代码由工具生成。
//     对此文件的更改可能会导致不正确的行为
// </auto-generated>
//------------------------------------------------------------------------------
using Chloe.Annotations;
using System.ComponentModel;
using System.Data;
namespace @(string.IsNullOrWhiteSpace(Model.Module)?"Gksyb.Model":$"{Model.Module}.Entities")
{
    /// <summary>
    /// 实体类@(Model.Name)
    /// </summary>
    [Table("@Model.Name")]
    public class @Model.Name
    {
@foreach(var item in Model.Columns)
{
    var csType = item.CsType == "string" ? item.CsType : $"{item.CsType}?";
    var comment = item.Comment ?? item.Name;
    var attributes = new List<string>();
    if (item.IsPrimary == true) attributes.Add("IsPrimaryKey = true");
    if (item.CsType == "string")attributes.Add("DbType = DbType.AnsiString");
    var columnAttribute = attributes.Count < 1 ? "" : $"\r\n        [Column({attributes.ToStr(", ")})]";
        @:/// <summary>
        @:/// @(comment)
        @:/// </summary>
        @:[Description("@(comment)")]@columnAttribute
        @:public @csType @item.Name { get; set; }@Environment.NewLine
}
    }
}